// OPERADORES LÓGICOS (LOGICAL OPERATORS)

// - 2 valores booleanos, quando verificados, resultará em verdadeiro ou falso

let pao = true
let queijo = false

// AND - && - cofirma ambos os valores (E)
// console.log(pao && queijo) - logo 'false'

// OR || - confirma pelo menos um dos valores (OU)
// console.log(pao || queijo) - logo 'true'

// NOT ! - diferencia os valores - (nega) - OPERADORES DE NEGAÇÃO
//console.log(!pao)